stages:
  - build

build:
  image: acm-registry:5000/node:10.16.3
  stage: build
  variables:
    GIT_STRATEGY: fetch
  tags:
    - build_front
  only:
    - master
  script:
    - node --version
    - npm install
    - npm run build
  artifacts:
    name: artifact1
    paths:
      - build/
    expire_in: 1 day

